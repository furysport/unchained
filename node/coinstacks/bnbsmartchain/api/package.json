{
  "name": "@sudophunk/bnbsmartchain-api",
  "version": "13.0.0",
  "license": "MIT",
  "private": true,
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn generate && tsc",
    "clean": "rm -rf dist node_modules src/routes.ts src/swagger.json",
    "generate": "tsoa spec-and-routes"
  },
  "dependencies": {
    "@sudophunk/blockbook": "^13.0.0",
    "@sudophunk/common-api": "^13.0.0"
  }
}
